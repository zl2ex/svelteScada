import { users } from "./user";
import { tags } from "./tag";
import { folders } from "./folder";
import { devices } from "./device";
import { udts } from "./udt";

export const collections = {
  users: users,
  tags: tags,
  folders: folders,
  devices: devices,
  udts: udts,
};
